<h2><%= user.username %></h2>
<% if((locals.isLoggedIn)){%>
  <% console.log(locals.user.username, user.username) %>
  <% if((locals.user.username === user.username)){%>
    <a href="/users/<%= user.id %>/edit">Edit</a>
  <% } %>
<% } %>
